const radius = 10;
const height = 15;

const volumeCilinder = Number((Math.PI * Math.pow(radius, 2) * height).toFixed(2));

console.log(`Volume of cilinder: ${volumeCilinder}`);
console.log(typeof volumeCilinder);